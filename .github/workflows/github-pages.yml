name: Deploy static content to Pages

on:
  push:
    branches: ["main"]
  # pull_request:
  #   branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets the GITHUB_TOKEN permissions to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  # Single deploy job since we're just deploying
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Setup .NET Core SDK
        uses: actions/setup-dotnet@v1
        with: 
            dotnet-version: 8.x
            include-prerelease: true
            
      - name: Publish .NET Core Project
        run: dotnet publish AgeCalculatorApp/AgeCalculatorApp/AgeCalculatorApp.csproj -c Release -o release --nologo
      
# https://github.com/SteveSandersonMS/BlazorOnGitHubPages

      - name: Debug working directory
        run: 
            ls -al

      - name: Debug release
        run: 
            ls -al release/wwwroot

      # - name: copy index.html to 404.html
      #   run: cp 404.html release/wwwroot/404.html

      # - name: Add .nojekyll file
      #   run: touch release/wwwroot/.nojekyll
    
      # - name: Commit wwwroot to Github Pages
      #   uses: JamesIves/github-pages-deploy-action@3.7.1
      #   with:
      #       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #       BRANCH: gh-pages
      #       FOLDER: release/wwwroot

      - name: Commit wwwroot to Github Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
            github_token: ${{ secrets.GITHUB_TOKEN }}
            publish_dir: release/wwwroot

      # - name: Setup Pages
      #   uses: actions/configure-pages@v4

      # - name: Upload artifact
      #   uses: actions/upload-pages-artifact@v3
      #   with:
      #     # Upload wwwroot folder
      #     path: './release/wwwroot'

      # - name: Deploy to GitHub Pages
      #   id: deployment
      #   uses: actions/deploy-pages@v4
